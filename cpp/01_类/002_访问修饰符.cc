#include <iostream>

using namespace std;

/*
public ： 公有成员在程序中和类的外部都是可以访问的

private ： 私有成员只有在当前类中，或友元函数中可以访问

protected ： 保护成员与私有成员非常类似，但有一点不同，保护成员在派生类中是可以访问的

不加任何修饰符情况下，默认类的所有成员都是私有的
*/

int main()
{
  
  return 0;
}