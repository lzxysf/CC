tcp是面向连接的，udp是无连接的。
tcp是有客户端和服务端的，需要accept、connect，udp没有严格的服务端客户端的区分。
udp除了可以一对一的发送数据（单播），还有广播和组播的概念，而tcp是没有广播组播的概念的。
广播是指局域网的一个主机发送数据会抵达局域网内的所有主机，
组播是局域网一个主机发送数据抵达局域网内的一个组中的所有主机。

日常生活中所谓的路由器，其实包含了交换机和路由器，交换机负责局域网内的数据转发，路由器负责局域网与外网的数据转发。
